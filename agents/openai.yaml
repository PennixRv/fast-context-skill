interface:
  display_name: "Fast Context"
  short_description: "Route genuinely ambiguous code-location questions after local tools and CodeGraph"
  default_prompt: "Route only genuinely ambiguous business, historical, or legacy code-location questions here after local tools and CodeGraph cannot locate useful candidates; verify every returned path locally."

policy:
  allow_implicit_invocation: true
