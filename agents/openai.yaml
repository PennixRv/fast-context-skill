interface:
  display_name: "Fast Context"
  short_description: "On-demand semantic code candidates"
  default_prompt: "Use $fast-context only after local search and CodeGraph cannot identify the relevant code location."

policy:
  allow_implicit_invocation: false
